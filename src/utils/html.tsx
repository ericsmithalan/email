export const htmlTemplate = <P,>(doc: Document, props: P) => {
    // var blob = new Blob([html], {type: 'text/html'});
    // var iframe = document.querySelector("iframe");
    // iframe.src = URL.createObjectURL(blob);
};
