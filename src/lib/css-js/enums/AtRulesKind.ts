import { toEnum } from "../utils/toEnum";

export const AtRulesKind = toEnum([
    "@charset",
    "@document",
    "@font-face",
    "@keyframes",
    "@media",
    "@page",
    "@supports",
    "@viewport",
]);
