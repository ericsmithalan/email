import { toEnum } from "../utils/toEnum";

export const CssAtRulesKind = toEnum([
    "@charset",
    "@document",
    "@font-face",
    "@keyframes",
    "@media",
    "@page",
    "@supports",
    "@viewport",
]);
