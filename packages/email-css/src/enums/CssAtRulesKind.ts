import { strEnum } from "../utils/strEnum";

export const CssAtRulesKind = strEnum([
    "@charset",
    "@document",
    "@font-face",
    "@keyframes",
    "@media",
    "@page",
    "@supports",
    "@viewport",
]);
